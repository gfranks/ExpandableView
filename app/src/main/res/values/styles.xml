<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/theme_gray</item>
        <item name="android:colorPrimaryDark">@color/theme_gray_dark</item>
        <item name="android:windowBackground">@color/theme_gray_light</item>

        <item name="colorPrimary">@color/theme_gray</item>
        <item name="colorPrimaryDark">@color/theme_gray_dark</item>
    </style>

</resources>
